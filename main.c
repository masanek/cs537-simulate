#include "job_loader.h"

int main()
{
    int clock;
    /*Initialize all the Managers*/
    /*Grab the first job?*/
    /*Enter the main loop*/
    for(clock = 0; clock < 20; clock++)
    {
        next_job(clock);
    }
    return 0;
}
