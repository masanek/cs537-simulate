#include <stdio.h>

#include "job_loader.h"

void next_job(int current_time)
{
    /*read in the next line*/
    /*parse it and build the job*/
    /*return if job would have arrived*/
    printf("%i\n",current_time);
}
